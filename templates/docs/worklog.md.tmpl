<!-- ROLE: the narrative worklog — the authored, chronological record of what was tried each
     session and why. The one artifact the generation pipeline never produces: it is the human
     (and orchestrator) narrative, the reasoning trail behind the machine-checked record.
     UPDATE POLICY: authored-append-only. Add a new dated session entry at the top-level "Sessions"
     list; never rewrite a past entry. Corrections are new entries that cite the earlier one.
     TRIGGER: a work session (append its entry at session close, alongside the HANDOFF rewrite). -->

# Worklog — {{RK_SLOT_PROJECT_NAME}}

Chronological narrative. Append one entry per session; never rewrite a past entry.

## Sessions

<!-- newest first; append above the previous entry:

### [YYYY-MM-DD] session title
- Direction taken and why (cite the `fr` arm / mechanism tag).
- What was banked, refuted, or left open.
- Deviations from the exploration recommendation, with reasons.
-->
