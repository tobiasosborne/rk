<!-- ROLE: the conventions ledger — notation, naming, and the reasoning behind each convention
     fixed for this campaign. The single source of truth for "how we write things here", so a
     later agent adopts a convention instead of reinventing a conflicting one.
     UPDATE POLICY: authored-append-only. Add a new dated entry when a convention is fixed or
     revised; never rewrite or delete a settled entry (its reasoning is the standing record of why
     the convention exists). A superseding entry cites the one it replaces.
     TRIGGER: a new notational or naming convention is adopted, or an existing one is revised. -->

# CONVENTIONS — {{RK_SLOT_PROJECT_NAME}}

Each entry fixes one convention and records why. Append; do not rewrite.

## Notation

<!-- e.g. symbols, operator conventions, index ranges — one bullet per fixed choice, with reason -->

## Naming and ids

Shard ids use the prefix `{{RK_SLOT_SHARD_PREFIX}}-` with the layer convention
(`{{RK_SLOT_SHARD_PREFIX}}-def-<slug>`, `-lem-`, `-thm-`, `-prop-`, `-cor-`, `-op-`, `-obs-`).
Ids are stable and greppable; a rename is a deliberate, logged act.

## Ledger

<!-- append dated entries below: [YYYY-MM-DD] convention — reasoning -->
