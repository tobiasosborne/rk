<!-- ROLE: the gotchas-and-dead-routes ledger — things easy to get wrong, refuted directions with
     their death certificates, and live faithfulness callouts. Read before touching a flagged item
     or re-walking a route. Retracted rigorous claims, once any exist, are recorded here too.
     UPDATE POLICY: authored-append-only. Add a dated entry when a route dies or a gotcha is found;
     never delete a death certificate (its value is stopping the next agent from re-walking the
     route). A revived route cites and supersedes its certificate rather than erasing it.
     TRIGGER: a direction is refuted, a subtle error is discovered, or a claim is retracted. -->

# FINDINGS — {{RK_SLOT_PROJECT_NAME}}

## Faithfulness callouts

Things easy to get wrong in this campaign. One bullet each; state which evidence a claim rests on.

## Dead routes

Each refuted direction with a falsifiable death certificate: what was tried, why it fails, and the
post-state (the "statability" — what is now known to be false or blocked). A `died` outcome without
a falsifiable post-state does not belong here.

<!-- append dated entries below: [YYYY-MM-DD] route — certificate (why it fails, post-state) -->

## Retractions

Claims once recorded as rigorous and later withdrawn, with the reason. Empty until the first
retraction; never rewrite history to hide one.
