<!-- ROLE: the current-state handoff — where the campaign is right now, the current work item, the
     next steps, and the audit-cycle counter. The first thing the next session reads after
     CLAUDE.md. Not a history: history lives in git and docs/worklog.md.
     UPDATE POLICY: rewritten-whole, at every session close. Rewrite, do not append; keep it short
     (a target of ~150 lines). A stale HANDOFF is worse than none.
     TRIGGER: session close, or any change that makes the "current work" line untrue. -->

# HANDOFF — {{RK_SLOT_PROJECT_NAME}}

## State

One-paragraph snapshot: what is established, what is in flight, what is blocked. Cite the
north-star contract's current status on the rigour ladder.

## Current work

The single work item in flight (with its `bd` id). What is done, what remains.

## Next steps

The concrete next actions, in order. File anything larger as a `bd` issue rather than describing
it here.

## Audit cycle counter

<!-- Constitutional (process) trigger in M1: this counter and log are maintained by hand, per
     CLAUDE.md §6's session-close ritual. `rk audit` (arrives in a later rk release, not in this
     binary — run `rk --help` for what exists today) is expected to mechanize the read-back, and
     possibly this bookkeeping, in M5. Until then an agent following CLAUDE.md §6 IS the
     mechanism — define cycle := one orchestrator session. -->

- Cycles-since-last-audit: **0**
- Audit cadence: {{RK_SLOT_AUDIT_CADENCE}} cycles (session close is BLOCKED when
  cycles-since-last-audit >= cadence; see `CLAUDE.md` §6).

### Audit log

Newest first. Recording a manual audit pass means appending one dated line here AND resetting
"Cycles-since-last-audit" to 0 above, in the same session-close edit (`CLAUDE.md` §6 step 2).

<!-- e.g.: "- [YYYY-MM-DD] cycle N: manual audit pass (overclaim / convention-drift / wandering
     reviewed). Notes: <one line>." -->
