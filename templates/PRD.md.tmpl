<!-- ROLE: WHAT this campaign is and what may be changed — the entry point, read first. Scope,
     the north-star contract, the decision record, success criteria, out-of-scope. HOW to work
     lives in CLAUDE.md; current state in HANDOFF.md.
     UPDATE POLICY: rewritten-whole. Revise the affected section as a whole when scope or a
     decision changes; do not append dated fragments. Settled decisions are not relitigated in
     passing — a decision change is a deliberate edit with a reason.
     TRIGGER: a scope change, a new or revised settled decision, or a change to a success bar. -->

# PRD — {{RK_SLOT_PROJECT_NAME}}

## 1. Mission

{{RK_SLOT_GOAL}}

The interface is an orchestrator agent governed by the constitution (`CLAUDE.md`) and enforced
by hooks and gates. Natural language is the medium of development; adversarial agent verification
is the correctness mechanism. The hard invariant: no hallucinated claims, no result stated above
its evidence level, ever.

## 2. North-star contract

{{RK_SLOT_NORTH_STAR}}

This is the single result the campaign is organized to establish or honestly refute. State its
current status honestly on the rigour ladder; it starts as an open problem.

## 3. Phase and process

**Current phase.** {{RK_SLOT_PHASE}}

Exploration is cheap, fast, lightly logged. Consolidation is contract-shaped, eagerly defined,
hard-tier verified, generated into a report. The transition toward consolidation is a logged,
deliberate act. See `CLAUDE.md` §3.

## 4. Decision record

Record here the settled decisions specific to this campaign (conventions, scope boundaries,
chosen mechanisms) so they are not relitigated. Each row: the question, the decision, the date.

| # | Question | Decision | Date |
|---|---|---|---|
|   |          |          |      |

## 5. Compute and model policy

**Compute budget.** {{RK_SLOT_COMPUTE_BUDGET}}

**Model policy.** {{RK_SLOT_MODEL_POLICY}}

## 6. Success criteria

State the concrete, checkable bars that decide whether the campaign succeeded. Numbers, not
adjectives.

## 7. Out of scope

State explicitly what this campaign will not attempt, so effort is not spent relitigating it.
