<!-- ROLE: current state + next steps. UPDATE POLICY: rewritten whole at session close,
     ≤150 lines. TRIGGER: read at session start (item 4 of the CLAUDE.md read order). -->

# HANDOFF

## State (2026-08-18, session close — campaign-D w1s16 AND w1s17 orchestrated FULLY DELEGATED same sitting; CRITERION 2 MET)

One Fable session (dual role: rk observer + campaign-D orchestrator; TJO issued the sitting's
directives — "continue, do not escalate or stop until a full quantum PCP grade definition is
in place; prefer codex subagents, proportionally less opus, no fable subagents, sonnet for
code queries" — then departed; the entire cycle ran under decisions 15+16) orchestrated
campaign w1s16 in ../rk-campaign-D end to end:

- **The SECOND proved-mod-audit result, on the FIRST fully-delegated promotion chain**:
  `prop-word-power-commuting-ff-instantiation` — Theorem P1 bites non-vacuously on an
  explicit commuting frustration-free n-qubit family, exact resource receipt incl. exhibited
  DE-v1 description packages — closed at sha 75d80bf1, Gate 8 accepts, fr-banked under a new
  mechanical `provenance-chain` oracle. First exercise of decision 16's PER-CLAIM pairing:
  prover-of-record codex, verification + three endorsing passes claude-opus (four distinct
  sessions; two REFUSALS on real grounds on the record; one endorsement SUPERSEDED-not-voided
  when the banking gate demanded a machine-readable prover: seam). Criterion 2's local-class
  residue DISCHARGED at the rigorous rung. The harness permission layer did NOT hold this
  status write (unlike P1's) — recorded as fact in-shard and in campaign FINDINGS.
- **The campaign's named open problem ANSWERED at stated**: constant gap at o(N) degree —
  `thm-direct-sum-qutrit-product-constant-gap`, triangle summands over F_3^t direct-summing
  to M_{K_3}^(tensor t), exact gap 1/2 at every t with D/N = (2/3)^t. Elementary, graded
  PASS by review N. New frontier: constant gap + constant locality + bounded qudit dim
  simultaneously (campaign bead -rqs).
- **Review N** (single round review, ACCEPT-WITH-REPAIRS, anti-Zeno held): systemic
  description-size defect found by two independent passes and TYPED at definition level
  (DE-v1, exact code after repair); adoption of the direct-sum candidate DENIED twice on the
  strict completeness reading — route is theorem-level DE-v1 bounds (campaign -kba). Two
  orchestrator faithfulness DEFECTs found and repaired same sitting (constitution §4
  contradiction; one detached dispatch — handling ruled FAITHFUL).
- **PRD decision 16 recorded**: TJO's model-roster amendment (codex-preferred lanes, sonnet
  for code queries, no Fable workers, per-claim cross-vendor pairing). TJO's qPCP aspiration
  (the 2005 Hastings bet) recorded WITHOUT scope change — PRD §7 stands.

**rk-side: the review-kill mystery is OPERATIONALLY SOLVED (rk-qe0j).** Cross-session
forensics with peer Claude session initech-os-50 (SendMessage protocol, second-precision
timeline both sides): a NEW codex exec attach reaps OTHER sessions' existing codex children
(~22 s, newcomer spared). NOT oom / resume races / network / global daemon failure — all
excluded with data. One solo kill (22:09:28Z) stays honestly open with a complete exclusion
list. STANDING PRACTICE: serialize codex windows across sessions via SendMessage — 4/4 clean
inside windows vs 3 kills outside, this sitting; retry-survives now 7/7 lifetime. Durable
fix filed as a new rk feature bead (host-wide dispatch lockfile at codex launch; the peer
pre-committed to honoring it). One §4d incident this sitting: an orchestrator dispatch
nested codex behind `&` (detached) — caught in seconds, killed by verified PID, disclosed,
ruled by review N (violation; handling FAITHFUL); a process-safeguard bead is filed
campaign-side (dispatcher rejects backgrounding syntax).

w1s17 (same sitting, the seventh cycle): review O RULED PRD §6 CRITERION 2 FORMALLY MET
(the first pillar of the decision-15 target) on the promoted instantiation; the -rqs
question CLOSED BOTH WAYS (the locality-gap no-go obs-direct-sum-locality-gap-no-go, gap
<= 2k/n_* degree-independent, order-tight at 3/log2 N — locality is the currency, ruled NO
PRD tension); the Dinur capture REDUCED TO TWO RESIDUES (def-walk-indexed-neighbourhood-cloud
+ four shards, claude-opus lane; block-encoding necessity proved for the commuting class —
the lane's broader claim narrowed by review O, orchestrator overclaim owned and corrected);
DE-v1 one fresh pass from consensus (GATE semantic fixed; my fr oracle ruled UNSOUND and
hardened per prescription, genuine record re-verified). Review O ACCEPT-WITH-REPAIRS, one
repair wave, anti-Zeno held.

Campaign state at close: 92 argument shards (TWO proved-mod-audit) + 19 definitions; 79
bundles / 76 ledgered; review records A-O; audit counter 7/10; rk check green, zero ERRORs
(campaign HEAD 3e8a84a). Vendor bundle refreshed at 3e8a84a. rk-side: no rk source changes.

## Current work

- **rk-qqee (in_progress)**: observer watch. Campaign w1s18 opens on: (1) DE-v1's consensus
  pass then -kba then U adoption pass 3; (2) the capture residues -1v7/-j10 + the
  cross-cutting criterion-6 gate; (3) capture 4/4 (drsq/zigzag/replacement — the last
  untouched decision-15 pillar); (4) -ngt/-lfa/-0en; frozen-P1 erratum -bc6 for TJO.
  Round 18 sync at open.
- Governance held: anti-Zeno (one review + one repair); ~19 worker dispatches (11 codex, 4
  claude-opus verification/endorsement, zero collisions, zero fabricated identities); every
  orchestrator mechanical edit stated; two faithfulness defects self-surfaced to review N.

## Next steps

1. Campaign w1s19 per the campaign HANDOFF: banking attempt 2 (shas bc4bc567/d2196274/e730ae7c) + review P (alpha admissibility, clause-(d) ruling).
2. rk-side new P2 feature bead: host-wide codex dispatch lockfile (kill-pattern durable fix;
   peer session pre-committed) — natural wave-3 worker-contract work (rk-4w2y adjacent).
3. rk-side P2 items unchanged: rk-pxkk / rk-ahe9 / rk-40k2 citation-kind schema.
4. Wave 3 — worker contract / unattended operation (rk-4w2y, rk-p037, rk-j8xo), then wave 4
   (rk-5man), wave 5 (rk-czzc/rk-g7fc + Tier C batch). rk-mief and rk-afyf still gate
   campaign C window 2 if unattended.
5. rk-0s3u (P3) remains open, unblocking no one.

## TJO decision queue (rk-side, carried)

1. rk-cz1h memo §6.1 — four questions.
2. rk-23pr — ratify remaining autonomy plan items.
3. rk-mief — campaign C: attest backfill vs waiver for the 6 window-1 closes.
4. Roster policy: window-5 same-family waiver — standing or per-campaign?
5. Campaign codas (decaying): rk-2h33, rk-iup9, rk-mxl3.
6. NEW (FYI, not blocking): the campaign promotion's status write was NOT permission-held
   this time (P1's was) — the safety property now demonstrably rests on the endorsing chain,
   which refused once on real grounds before endorsing; recorded in-shard, in campaign
   FINDINGS, and here. If TJO wants the permission hold restored as a hard gate, that is a
   harness-config decision, not a campaign one.

## Key facts for the next session

- **Corpus counts are 176** in all three places (test/corpus.test.ts title+assertion,
  EXPECTED_FIXTURE_COUNT, corpus/README.md Totals). Keep them in step.
- Campaign-D roster (decision 16): orchestrator Fable (sole seat); lanes codex gpt-5.6-sol
  preferred, claude-opus-5 reduced proportion; code queries sonnet; reviews codex xhigh;
  VERIFIERS PAIR PER CLAIM (opposite family from the prover-of-record; the banking gate
  checks it — a shard needs a machine-readable prover: frontmatter seam from creation).
- Codex kill pattern: see rk-qe0j (solved operationally). Protocol: serialize windows with
  active peers via SendMessage; never launch behind `&`; foreground-in-tracked-task; stdin
  from /dev/null; retry immediately on a kill (7/7 survive).
- Codex review invocation: `codex exec -s read-only -c model_reasoning_effort="xhigh" -o
  <scratch> "<prompt>" < /dev/null`, backgrounded as a tracked task, timeout 5400.
- Campaign probe-channel exit codes (1.8.0): 2 validation, 3 output exists, 4 already
  ledgered, 5 busy (retry safe), 6 poisoned. Ledger ts NON-MONOTONE; order by append
  position (rk-ahe9). Heredoc writes CAN fail silently — always verify with a read-back.
- `make refresh-bundles` runs FROM THE rk REPO ROOT (bundles ../rk-campaign-D).
- Repo self-contained since fed740c; full suite still needs `../vibefeld` (2 seam tests).

## Governance (standing)

- L1/L2 never relaxed; corpus 176/176 + selftest are the standing guard between reviews.
- D1-D8 + Amendment A1 stand. bd for all tracking. Campaign A wound down, B closed, C
  between windows 1 and 2, D mid-window-1 (w1s11-w1s17 closed and committed; audit counter
  7/10; TWO proved-mod-audit results + CRITERION 2 FORMALLY MET; w1s18 opens on the DE-v1
  consensus pass + -kba + the capture residues; sole Fable seat; codex-preferred lanes per decision 16; codex xhigh
  reviews; observer = rk main session; TJO continue-authorization standing — campaign PRD
  decisions 15+16).
