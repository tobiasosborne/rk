The source-level validity changes pass the selftest corpus, but the deployed binary still exhibits the old false-green behavior. The newly added operational guidance also contains incorrect exit-code and remediation instructions, and required handoff state was not updated.

Full review comments:

- [P1] Rebuild and reinstall the binary with the new gate — /home/tobias/Projects/rk/src/gates/refs-extraction.ts:151-157
  For any campaign invoking the compiled `rk`, this fix is not active: `dist/rk check --root corpus/refs/refs-21/repo` currently exits 0 with `checked 1/1`, while `bun run src/cli.ts check ...` exits 1 on the violated pin, and `~/.local/bin/rk` is byte-identical to the stale `dist/rk`. Recompile and reinstall after these validity changes; the required build gate is documented in [AGENTS.md:152-155](/home/tobias/Projects/rk/AGENTS.md#L152-L155) and must precede commits per [AGENTS.md:162-164](/home/tobias/Projects/rk/AGENTS.md#L162-L164).

- [P2] Return status 2 for missing probe arguments — /home/tobias/Projects/rk/templates/CLAUDE.md.tmpl:288-291
  When either required positional argument is omitted, the script's `${1:?}`/`${2:?}` expansions at `templates/runs/probe-channel.sh.tmpl:60-61` terminate Bash with status 1 before anything is ledgered. The new text instead classifies invalid usage as 2 and every status outside 2–6 as a ledgered probe result, so callers can mistake a usage failure for a completed run; validate the argument count explicitly and exit 2, or document status 1 as non-ledgering.

- [P2] Permit retry after an exit-5 lock timeout — /home/tobias/Projects/rk/templates/CLAUDE.md.tmpl:300-301
  For an exit 5 caused by `flock` timing out, the script exits at `templates/runs/probe-channel.sh.tmpl:105-109`, before the bundle reservation checks or any write, so the same bundle remains unused and safe to retry. These lines contradict the immediately preceding “Retry later” instruction and will make workers abandon valid bundles; distinguish this case from exits that actually consume or poison the bundle.

- [P2] Give af-canonical retractions an accurate remedy — /home/tobias/Projects/rk/src/reward/pma-withdrawal.ts:58-60
  When the refusal comes from an `af-canonical` retraction, editing and re-verifying cannot currently release it: `readRetractionFacts` deliberately treats every such record as live because rk cannot observe the current af hash (`src/gates/linker-retraction.ts:122-129`). The new remedy therefore sends the operator through a repair that remains blocked; make the message domain-specific and explain the known af-canonical limitation.

- [P2] Refresh HANDOFF for the landed 176-fixture state — /home/tobias/Projects/rk/src/corpus/discovery.ts:192-192
  This change lands rk-r0j3, rk-o85b, and rk-yic3 and raises the corpus count to 176, but `HANDOFF.md:59-67` still lists those items as future work and `HANDOFF.md:84-85` still asserts 172 fixtures. That stale cross-session state will direct the next session toward completed work and violates the docs-move requirement in [AGENTS.md:66-67](/home/tobias/Projects/rk/AGENTS.md#L66-L67); rewrite HANDOFF with the landed state.