<!-- ROLE: review record (authored by external reviewer). UPDATE POLICY: immutable body.
     TRIGGER: M0.3 milestone review round 3 (rk-4wm), first under the two-list policy. -->

# M0.3 milestone review round 3 — codex gpt-5.6-sol, xhigh

Reviewer: GPT 5.6 Sol via codex exec (read-only sandbox), 2026-07-18. Round 3.
Verdict: RED — narrowed. Landing-blockers: 4 (one loader/snapshot cluster).
Follow-ups: 6 (batched, non-gating). Aggregate-flood ruling: RATIFIED.
AISM zero-rk-bug: PASS. Robustness: PASS.

# RED

Current HEAD: `d877af8413b5dbb6e343cde3463259e4fe7964a2`.

## Disposition

| Item | Disposition | HEAD evidence |
|---|---|---|
| N1 — global present-file hashing | **partially-fixed** | Ordinary untracked files outside include rules are hashed, but the walk skips four directory basenames at [load.ts:47](/home/tobiasosborne/Projects/rk/src/gates/load.ts:47) and [load.ts:163](/home/tobiasosborne/Projects/rk/src/gates/load.ts:163). Missing hashes become absent/unverifiable WARNs at [provenance-checks.ts:44](/home/tobiasosborne/Projects/rk/src/gates/provenance-checks.ts:44). The normative text also retains the contradictory old exception at [gate-contracts.md:773](/home/tobiasosborne/Projects/rk/docs/gate-contracts.md:773). |
| N2 — required `SnapshotFacts` | **partially-fixed** | Facts are required by the type and accessors no longer fall back at [snapshot.ts:17](/home/tobiasosborne/Projects/rk/src/gates/snapshot.ts:17) and [snapshot.ts:40](/home/tobiasosborne/Projects/rk/src/gates/snapshot.ts:40). However, `snapshotFromFiles` defaults every text path to tracked while defaulting hashes to empty at [snapshot.ts:85](/home/tobiasosborne/Projects/rk/src/gates/snapshot.ts:85) and [snapshot.ts:108](/home/tobiasosborne/Projects/rk/src/gates/snapshot.ts:108), recreating a present/tracked path that Gate 4 interprets as absent. |
| N3 — invalid include denominator | **fixed** | Invalid targets enter `invalidIncludes` and `nonConforming` at [shards.ts:126](/home/tobiasosborne/Projects/rk/src/gates/shards.ts:126), then the denominator union at [shards.ts:253](/home/tobiasosborne/Projects/rk/src/gates/shards.ts:253). `shards-07` pins `0/1` at [expected.json:12](/home/tobiasosborne/Projects/rk/corpus/shards/shards-07/expected.json:12). |
| N4 — missing frontmatter-invalid corpus path | **partially-fixed** | `provenance-17` now pins the aggregate WARN and honest `1/2` denominator at [expected.json:4](/home/tobiasosborne/Projects/rk/corpus/provenance/provenance-17/expected.json:4). The coverage ledger still says seven asserted fixtures and 79 remaining at [corpus/README.md:318](/home/tobiasosborne/Projects/rk/corpus/README.md:318) and [corpus/README.md:335](/home/tobiasosborne/Projects/rk/corpus/README.md:335), omitting coverage-bearing `shards-07`. |
| N5 — fixture-table and divergence-ledger drift | **fixed** | Added fixtures are present in the contract tables, including linker at [gate-contracts.md:503](/home/tobiasosborne/Projects/rk/docs/gate-contracts.md:503), provenance at [gate-contracts.md:899](/home/tobiasosborne/Projects/rk/docs/gate-contracts.md:899), runs at [gate-contracts.md:982](/home/tobiasosborne/Projects/rk/docs/gate-contracts.md:982), and shards at [gate-contracts.md:1151](/home/tobiasosborne/Projects/rk/docs/gate-contracts.md:1151). `linker-24` now carries empirical AISM behavior and triage at [expected.json:12](/home/tobiasosborne/Projects/rk/corpus/linker/linker-24/expected.json:12); totals reconcile to 86 at [corpus/README.md:116](/home/tobiasosborne/Projects/rk/corpus/README.md:116). |
| N6 — impure corpus runner under `src/gates` | **fixed** | The runner is now explicitly an edge under `src/corpus` at [run.ts:1](/home/tobiasosborne/Projects/rk/src/corpus/run.ts:1). The rewritten scan examines the complete leading comment block at [selftest.ts:55](/home/tobiasosborne/Projects/rk/scripts/selftest.ts:55) and separately checks every current direct `src/gates` file at [selftest.ts:128](/home/tobiasosborne/Projects/rk/scripts/selftest.ts:128). |
| N7 — `rk check --selftest` | **fixed** | Dispatch is wired at [check.ts:113](/home/tobiasosborne/Projects/rk/src/cli/check.ts:113), runs the shared corpus runner at [check.ts:100](/home/tobiasosborne/Projects/rk/src/cli/check.ts:100), and is exposed in help at [cli.ts:28](/home/tobiasosborne/Projects/rk/src/cli.ts:28). The current checkout produced 86/86. An incomplete-corpus false-green remains below as a new follow-up. |
| N8 — per-fixture crash boundary | **fixed** | JSON parsing, loading, gate execution, and assertions are inside one boundary at [run.ts:87](/home/tobiasosborne/Projects/rk/src/corpus/run.ts:87), with exceptions converted into fixture errors at [run.ts:163](/home/tobiasosborne/Projects/rk/src/corpus/run.ts:163). Continuation is pinned at [run.test.ts:52](/home/tobiasosborne/Projects/rk/test/corpus/run.test.ts:52). |
| N9 — crash sentinel path | **fixed** | The exception is documented at [framework.ts:18](/home/tobiasosborne/Projects/rk/src/gates/framework.ts:18), implemented as `<gate:NAME>` at [check.ts:60](/home/tobiasosborne/Projects/rk/src/cli/check.ts:60), and blessed normatively at [gate-contracts.md:72](/home/tobiasosborne/Projects/rk/docs/gate-contracts.md:72). |
| Round-1 partial 1 | **partially-fixed** | Raw-byte hashing is fixed, but the skip-set retains the same stale-source false-WARN class; see N1 and [load.ts:163](/home/tobiasosborne/Projects/rk/src/gates/load.ts:163). |
| Round-1 partial 2 | **fixed** | Real snapshots record directory existence, empty run bundles are enumerated from facts at [runs.ts:60](/home/tobiasosborne/Projects/rk/src/gates/runs.ts:60), and absent `report/sections` is checked at [shards.ts:88](/home/tobiasosborne/Projects/rk/src/gates/shards.ts:88). |
| Round-1 partial 4 | **fixed** | Skips are named and counted at [provenance-parse.ts:79](/home/tobiasosborne/Projects/rk/src/gates/provenance-parse.ts:79), consumed in coverage at [provenance.ts:201](/home/tobiasosborne/Projects/rk/src/gates/provenance.ts:201), and now corpus-pinned by `provenance-17`. |
| Round-1 partial 6 | **partially-fixed** | Selftest runs all fixtures, but coverage validation remains conditional at [run.ts:134](/home/tobiasosborne/Projects/rk/src/corpus/run.ts:134). Seventy-eight of 86 fixtures have no coverage assertion, including all 24 linker fixtures. |
| Round-1 partial 8 | **fixed** | Gate 4 emits one aggregate finding at [provenance.ts:132](/home/tobiasosborne/Projects/rk/src/gates/provenance.ts:132); v3 records a maximum of ten findings per check at [v3.md:191](/home/tobiasosborne/Projects/rk/docs/reviews/2026-07-18-aism-divergence-triage-v3.md:191). This disposition depends on the ratification below. |

## Landing-blockers

1. `{severity: BLOCKER, file: src/gates/load.ts:47, claim: Gate 4 still false-PASSes some present stale sources as WARN-only.}`

   Evidence: `SKIP_DIRS` excludes every directory named `.git`, `node_modules`, `.hg`, or `.svn`, regardless of location, and the walker does not descend at [load.ts:47](/home/tobiasosborne/Projects/rk/src/gates/load.ts:47) and [load.ts:163](/home/tobiasosborne/Projects/rk/src/gates/load.ts:163). Thus a source such as `notes/node_modules/payload.bin`, outside the text include rules, receives no hash. Gate 4 treats `undefined` as genuinely absent and aggregates it into a WARN at [provenance-checks.ts:44](/home/tobiasosborne/Projects/rk/src/gates/provenance-checks.ts:44), contradicting present+mismatch ⇒ ERROR at [gate-contracts.md:764](/home/tobiasosborne/Projects/rk/docs/gate-contracts.md:764). The N1 test covers ordinary `notes/data.txt`, not a skipped basename, at [load.test.ts:134](/home/tobiasosborne/Projects/rk/test/load.test.ts:134).

   Disposition: the present/absent fact must not inherit a broad traversal skip-set. Hash referenced source paths independently or narrow the skip policy without reintroducing unverifiable-present paths; add a skipped-basename red fixture.

2. `{severity: MAJOR, file: src/gates/snapshot.ts:85, claim: the sanctioned builder still creates internally contradictory validity facts.}`

   Evidence: `snapshotFromFiles` defaults `tracked` to every supplied text path at [snapshot.ts:85](/home/tobiasosborne/Projects/rk/src/gates/snapshot.ts:85) and [snapshot.ts:108](/home/tobiasosborne/Projects/rk/src/gates/snapshot.ts:108), while `sha256` is optional and defaults empty at [snapshot.ts:87](/home/tobiasosborne/Projects/rk/src/gates/snapshot.ts:87) and [snapshot.ts:109](/home/tobiasosborne/Projects/rk/src/gates/snapshot.ts:109). Gate 4 equates the missing hash with genuine disk absence at [provenance-checks.ts:44](/home/tobiasosborne/Projects/rk/src/gates/provenance-checks.ts:44). A direct probe at HEAD produced `{has:true, tracked:true, hash:null}`.

   Production construction uses `loadSnapshot` at [check.ts:119](/home/tobiasosborne/Projects/rk/src/cli/check.ts:119) and [run.ts:91](/home/tobiasosborne/Projects/rk/src/corpus/run.ts:91), so the current CLI avoids this path. Two test sites still construct facts manually at [corpus.test.ts:35](/home/tobiasosborne/Projects/rk/test/corpus.test.ts:35) and [provenance.test.ts:37](/home/tobiasosborne/Projects/rk/test/gates/provenance.test.ts:37); both attach the fields, but the migration is not mechanically builder-only.

   Disposition: make coherent presence/hash facts mandatory for modeled present files—either require hashes, compute them at an appropriate edge, or represent presence separately. A builder advertised as producing a full `RepoSnapshot` must not default to an impossible edge state.

3. `{severity: MAJOR, file: src/gates/load.ts:125, claim: symlinks can escape the root, recurse through cycles, or crash the entire composed check before its exception boundary.}`

   Evidence: both walkers use `statSync`, which follows symlinks, at [load.ts:125](/home/tobiasosborne/Projects/rk/src/gates/load.ts:125) and [load.ts:161](/home/tobiasosborne/Projects/rk/src/gates/load.ts:161), then recurse without a realpath/visited set at [load.ts:130](/home/tobiasosborne/Projects/rk/src/gates/load.ts:130) and [load.ts:165](/home/tobiasosborne/Projects/rk/src/gates/load.ts:165). Only `readdirSync` is caught; a dangling or self-referential link can throw at `statSync`. `loadSnapshot` runs before `runGateSafely` at [check.ts:119](/home/tobiasosborne/Projects/rk/src/cli/check.ts:119), so all six coverage lines disappear, violating unconditional composition at [gate-contracts.md:95](/home/tobiasosborne/Projects/rk/docs/gate-contracts.md:95).

   Disposition: define and test a symlink policy using `lstat` plus cycle/root containment handling, and ensure snapshot-loading failures become a loud composed failure rather than an uncaught process exit.

4. `{severity: MAJOR, file: docs/gate-contracts.md:773, claim: the normative stale-source contract remains internally contradictory after N1.}`

   Evidence: [gate-contracts.md:764](/home/tobiasosborne/Projects/rk/docs/gate-contracts.md:764) says every present mismatched file is an ERROR regardless of tracking/include rules. Nine lines later, [gate-contracts.md:773](/home/tobiasosborne/Projects/rk/docs/gate-contracts.md:773) still defines an “untracked and outside the include rules” file as absent/WARN. That is the exact contradiction identified in round 2. Contract documents are Tier A at [CLAUDE.md:93](/home/tobiasosborne/Projects/rk/CLAUDE.md:93).

   Disposition: settle the paragraph consistently on the ratified rule—present stale ⇒ ERROR; genuinely absent ⇒ WARN—and align comments/fixtures with that single definition.

## Follow-ups

1. `{severity: MAJOR, file: src/corpus/run.ts:134, claim: the corpus still does not universally protect the mandatory coverage-line contract.}`

   Evidence: coverage shape and values are checked only inside `if (expected.coverage)` at [run.ts:134](/home/tobiasosborne/Projects/rk/src/corpus/run.ts:134). No linker fixture supplies that field, so removing linker coverage entirely would not fail `rk check --selftest`. The contract requires exactly one line from every gate at [gate-contracts.md:82](/home/tobiasosborne/Projects/rk/docs/gate-contracts.md:82).

   Disposition: universally assert exactly one correctly named coverage entry; keep exact numeric/unit assertions limited to coverage-target fixtures if desired.

2. `{severity: MAJOR, file: src/cli/check.ts:89, claim: an existing but empty or structurally incomplete corpus can pass rk check --selftest.}`

   Evidence: `runSelftest` checks only whether the top-level `corpus/` directory exists at [check.ts:89](/home/tobiasosborne/Projects/rk/src/cli/check.ts:89). Missing gate directories become empty lists at [discovery.ts:23](/home/tobiasosborne/Projects/rk/src/corpus/discovery.ts:23), and the formatter reports `0/0` with zero errors at [report.ts:40](/home/tobiasosborne/Projects/rk/src/corpus/report.ts:40). The package selftest separately enforces 86 at [selftest.ts:193](/home/tobiasosborne/Projects/rk/scripts/selftest.ts:193), but the plan-required CLI does not.

   Disposition: fail when any of the six gate corpus directories is absent/empty and enforce the ledger count or an equivalent manifest.

3. `{severity: MAJOR, file: scripts/selftest.ts:122, claim: the purity rewrite knowingly blesses two impure files inside the constitutionally pure src/gates directory.}`

   Evidence: `load.ts` and `config-load.ts` are allowlisted at [selftest.ts:122](/home/tobiasosborne/Projects/rk/scripts/selftest.ts:122), while L3 says all of `src/gates` is pure at [CLAUDE.md:32](/home/tobiasosborne/Projects/rk/CLAUDE.md:32). The specific N6 regression is fixed; this is the already-filed `rk-7uc` relocation debt.

   Disposition: retain as a batched architecture follow-up under the two-list policy; complete `rk-7uc` rather than growing the allowlist.

4. `{severity: MINOR, file: scripts/selftest.ts:164, claim: purity coverage arithmetic counts violations as files.}`

   Evidence: the numerator is `checked.length - violations.length` at [selftest.ts:163](/home/tobiasosborne/Projects/rk/scripts/selftest.ts:163). One file with multiple forbidden matches can produce an understated or negative “files clean” numerator.

   Disposition: count unique violating files for coverage while retaining every per-line diagnostic.

5. `{severity: MINOR, file: corpus/README.md:318, claim: the coverage-expectation ledger is stale after N3.}`

   Evidence: the table at [corpus/README.md:318](/home/tobiasosborne/Projects/rk/corpus/README.md:318) lists seven fixtures and omits `shards-07`, although `shards-07` has a coverage expectation at [expected.json:12](/home/tobiasosborne/Projects/rk/corpus/shards/shards-07/expected.json:12). Consequently “79 remaining” at [corpus/README.md:335](/home/tobiasosborne/Projects/rk/corpus/README.md:335) should be 78.

   Disposition: add `shards-07` and correct the arithmetic.

6. `{severity: MAJOR, file: CLAUDE.md:1, claim: CLAUDE.md and AGENTS.md are no longer byte-identical.}`

   Evidence: the header requires byte identity at [CLAUDE.md:1](/home/tobiasosborne/Projects/rk/CLAUDE.md:1). The current two-list policy appears at [CLAUDE.md:106](/home/tobiasosborne/Projects/rk/CLAUDE.md:106) but is absent from `AGENTS.md`; `AGENTS.md` also retains the older reviewer policy at [AGENTS.md:83](/home/tobiasosborne/Projects/rk/AGENTS.md:83). Commit `d877af8` changed only `CLAUDE.md`.

   Disposition: synchronize the two files in the next bookkeeping batch.

## Aggregate-flood ruling

**RATIFIED.**

The contract measures “findings emitted per check,” not affected identifiers. Gate 4 collects every whitelisted shard and performs exactly one `findings.push` at [provenance.ts:132](/home/tobiasosborne/Projects/rk/src/gates/provenance.ts:132). The unit test proves one finding while preserving the count and every sorted ID at [provenance.test.ts:467](/home/tobiasosborne/Projects/rk/test/gates/provenance.test.ts:467). This is structurally equivalent to the already-ratified frontmatter aggregate at [provenance-parse.ts:88](/home/tobiasosborne/Projects/rk/src/gates/provenance-parse.ts:88).

V3 therefore correctly applies the primary metric: maximum ten findings per check, without invoking the rejected single-root-cause exception, at [v3.md:191](/home/tobiasosborne/Projects/rk/docs/reviews/2026-07-18-aism-divergence-triage-v3.md:191) and [v3.md:205](/home/tobiasosborne/Projects/rk/docs/reviews/2026-07-18-aism-divergence-triage-v3.md:205). Structured path attribution is collapsed to the first shard, but the sorted IDs retain the decision-relevant identities; that does not turn one emitted finding back into 138 findings.

## Acceptance statement

Against the M0.3 bar at [IMPLEMENTATION_PLAN.md:77](/home/tobiasosborne/Projects/research-workflows/IMPLEMENTATION_PLAN.md:77):

- **Contract-correct gates: FAIL.** The hash skip-set retains a stale-source false-WARN, the required-facts builder permits an incoherent snapshot, symlinks can defeat composition, and the normative check-4 paragraph contradicts itself.
- **AISM HEAD zero-rk-bug divergence ledger: PASS.** AISM remains at `6baf521`; the live rerun produced 0 ERROR / 18 WARN, matching [v3.md:78](/home/tobiasosborne/Projects/rk/docs/reviews/2026-07-18-aism-divergence-triage-v3.md:78), and v3 records zero rk-bugs at [v3.md:237](/home/tobiasosborne/Projects/rk/docs/reviews/2026-07-18-aism-divergence-triage-v3.md:237).
- **Three-commit robustness without crash/flood: PASS.** Aggregate-flood semantics are ratified; v3 records no crashes and all coverage lines at [v3.md:146](/home/tobiasosborne/Projects/rk/docs/reviews/2026-07-18-aism-divergence-triage-v3.md:146), with maximum ten findings per check.
- **Selftest corpus: FAIL as a contract-completeness bar.** Both entry points currently report 86/86, but the new skipped-path and symlink failure modes have no red corpus protection, coverage remains only conditionally asserted, and the CLI can pass an incomplete corpus.

No files were changed; the worktree remains clean.