<!-- ROLE: Tier A review record — codex gpt-5.6-sol xhigh review of the rk-8805 branch (worktree-agent-a642886cb46efe3cc) vs master. AUTHORED, append-only; the orchestrator appends the repair-wave verification before the branch lands. TRIGGER: read before merging rk-8805 or at the next milestone review. -->

# Tier A review: rk-8805 (signature schema, interval entailment, Check 17, graph v3, bite core) — codex xhigh, 2026-08-20

Verdict on the first implementation: REJECT (6 MAJOR landing-blockers, 4 follow-ups); entailment core, red pair both directions, fixed-point permutation invariance, poset and null-bound handling CONFIRMED. Disposition: single repair wave by the implementing lane; orchestrator verifies mechanically; no re-review. Branch does not merge until the verification table below is complete.

## Review text (verbatim)

# REJECT

Six MAJOR validity findings block landing.

## Landing-blockers

1. **MAJOR — contradictory predicates can unlock dependency posts.** [`SignatureContext`](</home/tobiasosborne/Projects/rk/.claude/worktrees/agent-a642886cb46efe3cc/src/gates/signature-entail.ts:75>) stores repeated `(scope,key)` intervals separately, and [`keyEntailed`](</home/tobiasosborne/Projects/rk/.claude/worktrees/agent-a642886cb46efe3cc/src/gates/signature-profile.ts:250>) accepts when any one matches. A parent declaring both `qdim=const` and `qdim=poly` has zero vocabulary findings yet makes a `qdim=poly` dependency available and receives its post. Repair: conjoin repeated predicates via interval intersection—or reject duplicates—and ERROR on empty intersections before route evaluation.

2. **MAJOR — canonical identity is not unique.** [`canonicalValue`](</home/tobiasosborne/Projects/rk/.claude/worktrees/agent-a642886cb46efe3cc/src/gates/signature.ts:113>) sorts but does not deduplicate entries. A signature with one predicate and the same signature with that predicate duplicated are both accepted as canonical but have different bytes. Multi-key predicates can also be regrouped into two representations that are mutually `strongerOrEqual` yet have different stripped canonical text, falsifying the claimed antisymmetry. Repair: canonicalize to a deterministic per-scope/per-key representation, deduplicate/conjoin values, and add the constructed duplicate/regrouping cases.

3. **MAJOR — `required` is evadable through `kind`.** Only four kinds are signed at [`linker-signature.ts:28`](</home/tobiasosborne/Projects/rk/.claude/worktrees/agent-a642886cb46efe3cc/src/gates/linker-signature.ts:28>), while kind and status are independent enums at [`linker-lemma.ts:50`](</home/tobiasosborne/Projects/rk/.claude/worktrees/agent-a642886cb46efe3cc/src/gates/linker-lemma.ts:50>). Direct probe: `kind: open-problem`, `status: proved`, `signatures: required`, no block produced zero linker findings. Repair: enforce kind/status/af coherence and prevent exempt kinds from acting as proved/cited/validated results; add a red evasion fixture.

4. **MAJOR — real graph production silently drops signatures.** [`convertLemma`](</home/tobiasosborne/Projects/rk/.claude/worktrees/agent-a642886cb46efe3cc/src/graph/from-registry.ts:58>) never receives or emits the parsed signature. A signed shard passed through `parseRegistry → assembleGraphDocument` produced a v3 node with no `signature`; the golden is hand-authored and misses this path. Repair: thread parsed signatures through the registry conversion seam and add an authored-shard-to-v3 projection round-trip test.

5. **MAJOR — graph schema v3 rejects valid interval signatures.** [`regime`](</home/tobiasosborne/Projects/rk/.claude/worktrees/agent-a642886cb46efe3cc/schemas/graph.v1.json:146>) and predicate [`keys`](</home/tobiasosborne/Projects/rk/.claude/worktrees/agent-a642886cb46efe3cc/schemas/graph.v1.json:161>) allow strings only, while valid signatures and the v3 golden contain `[lo,hi]` arrays and `null` bounds. Repair: reuse the signature schema’s `predicateValue` union in graph v3 and validate the golden against the actual JSON Schema.

6. **MAJOR — bite’s advance clause accepts candidates that do not satisfy section 8.** The decomposition input contains only asserted member IDs and is never checked for actual route membership; [`advanceClause`](</home/tobiasosborne/Projects/rk/.claude/worktrees/agent-a642886cb46efe3cc/src/graph/bite.ts:239>) accepted a candidate with `memberIds: []` under clause (i). Clause (iii) at [`bite.ts:261`](</home/tobiasosborne/Projects/rk/.claude/worktrees/agent-a642886cb46efe3cc/src/graph/bite.ts:261>) also accepted a new-tool candidate whose existing post was not stronger, omitting section 8’s post-object condition. Repair: supply authoritative target routes/context, verify membership, and enforce every post object is new or strictly strengthened before any clause passes.

## Follow-ups

- [`routesOf`](</home/tobiasosborne/Projects/rk/.claude/worktrees/agent-a642886cb46efe3cc/src/gates/linker-signature.ts:115>) correctly unions unconditional deps into every route, and post-only objects are resolved, but neither behavior has a dedicated mutation-catching test. Add both cases.
- Under absent adoption, a present block is checked, but unsigned route members are collected at [`signature-entail.ts:233`](</home/tobiasosborne/Projects/rk/.claude/worktrees/agent-a642886cb46efe3cc/src/gates/signature-entail.ts:233>) and then omitted from findings and coverage. Surface the unsigned-member count explicitly.
- Changed source files exceed the 280-line cap: [`config.ts`](</home/tobiasosborne/Projects/rk/.claude/worktrees/agent-a642886cb46efe3cc/src/gates/config.ts:489>) is 489 lines and [`snapshot-load.ts`](</home/tobiasosborne/Projects/rk/.claude/worktrees/agent-a642886cb46efe3cc/src/store/snapshot-load.ts:289>) is 289. Split them.
- [`HANDOFF.md:84`](</home/tobiasosborne/Projects/rk/.claude/worktrees/agent-a642886cb46efe3cc/HANDOFF.md:84>) still says 176 fixtures; current ground truth is 188. [`types.test.ts:18`](</home/tobiasosborne/Projects/rk/.claude/worktrees/agent-a642886cb46efe3cc/test/graph/types.test.ts:18>) also still names graph version 2.

## Confirmed behavior

- The `lem-amp poly → thm-qpcp const` pair is rejected for the correct ambient `qdim` regime reason.
- The exact symmetric case—dependency `const`, parent `poly`—is also rejected.
- For individually consistent predicates, a post is added only after the dependency’s full `pre+regime` passes.
- Route order invariance is exhaustively tested over all 24 permutations of four members at [`signature-entail.test.ts:276`](</home/tobiasosborne/Projects/rk/.claude/worktrees/agent-a642886cb46efe3cc/test/gates/signature-entail.test.ts:276>).
- Poset incomparability and unbounded `null` endpoints behave conservatively. Open endpoints are not represented.
- Spectator matching is explicitly documented and tested as lexical/weak.
- Purity passed: 141/141 pure files and 39/39 gate files.
- `timeout 120 bun run selftest`: **OK, 188/188**.
- Focused new suites: **109/109 passed**.
- Full `bun test` was inconclusive in this read-only sandbox: 2661 passed, while temp-directory tests failed with `/tmp` `EROFS`; these were environment failures, not assertion failures.
## Repair-wave verification (orchestrator, 2026-08-21)

Single repair wave, implemented by a codex gpt-5.6-sol xhigh lane on branch `worktree-agent-a642886cb46efe3cc` (e686b49..5eb80da), following the repair plan in the bead comment (B2 before B1). Verified mechanically on the branch and again on master after merge; no re-review (anti-Zeno rule).

| Finding | Commit | Code | Fixtures / tests | Orchestrator check |
|---|---|---|---|---|
| B1 contradictory predicates (conjoined context) | 3b6b90f | `src/gates/signature-context.ts:49`, `src/gates/signature-profile.ts:232` | linker-59 | orchestrator: replaced the intersect-on-add with "keep prior" -> linker-59 RED (`signature-contradictory` absent); restored |
| B2 canonical uniqueness (per-scope grouping) | df30e10 | `src/gates/signature.ts:113` | linker-60, linker-61 | orchestrator: disabled value dedupe -> linker-60 RED; restored |
| B3 kind evasion (`kind-status-incoherent`, OR rule) | 9168db5 | `src/gates/linker-signature-policy.ts:12` | linker-62 | lane report; fixture green |
| B4 projection carries signature | bff5d29 | `src/gates/linker-parse.ts:224`, `src/graph/from-registry.ts:75` | test/graph/signature-graph-schema.test.ts | unit tests green |
| B5 graph v3 schema reuses predicateValue; test-only subset validator | f34d370 | `schemas/graph.v1.json:149,161`, `test/helpers/json-schema-subset.ts` | v3 golden + round-trip | unit tests green |
| B6 bite clauses (authoritative routes, every-provider post) | 217702a | `src/graph/bite.ts:174,198` | test/graph/bite.test.ts | unit tests green |
| follow-ups (unsigned-route count, 280-cap splits, stale test name) | 5eb80da | `src/gates/config-validation.ts`, `src/store/snapshot-include.ts` | — | `wc -l` under 280 on master for every touched file |

Design ruling surfaced by the lane, accepted by the orchestrator: `kind-status-incoherent` is gated on signatures being ADOPTED (`optional` or `required`), so a non-adopting repo's acceptance set is unchanged (blast radius zero). Residual carried to the next milestone review: the reviewer should confirm this adoption gate.

Gates: branch `bun test` 3205 pass / 0 fail, selftest 192/192; master after merge 3545 pass / 0 fail, selftest 246/246, compile OK.
