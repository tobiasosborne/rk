<!-- ROLE: plan-gap note (CLAUDE.md stop-condition discipline). AUTHORED.
     UPDATE POLICY: append-only. TRIGGER: M3.5 preflight abort, 2026-07-20. -->

# NOTE 2026-07-20 — M3.5 preflight abort: prover dispatch was never wired

## What happened

The M3.5 SC4-baseline live session (TJO-authorized spend) aborted at preflight with
**zero tokens spent**. The hard-tier live driver (`rk verify --af <id> --live`)
dispatches verifier turns only — `src/drive/driver-run.ts`'s header defers
"prover/seeding dispatch" to "a separate WP" — while the three staged baseline
workspaces (`../rk-m3.5-baseline`) are fresh, unseeded conjectures. Nothing to
verify; no machinery to produce the proofs. Full stop report:
`../rk-m3.5-baseline/STOP-REPORT-2026-07-20.md`.

Secondary finding: `isVerificationReady` (`src/drive/driver-plan.ts`) is a
`pending && !blocked` proxy that labels an unproven pending node "verification-
ready" while af itself reports "Prover: 1 nodes awaiting refinement / Verifier: 0
nodes ready for review". The dry-run therefore looked sane while the live premise
was unexecutable — the proxy's flagged limitation, live.

## Plan reading (why this is a mis-scoping, not a plan conflict)

- M3.2 acceptance: "A toy proof driven end-to-end with prover=claude/verifier=codex
  and the reverse" — prover-role dispatch demonstrated at backend level.
- M3.5: "re-proves 2–3 already-validated AISM lemmas from fresh workspaces" —
  requires proving, definitionally.
- M3.6 acceptance: "Re-proves one AISM lemma end-to-end".
- `src/drive/driver-prompts.ts` (M3.5-prep) already assembles prover AND verifier
  prompts.

The plan consistently expects prover dispatch; only the driver run-loop wiring is
missing. Resolution chosen: land prover dispatch in the driver (tracked as rk-gn4,
P1), with truthful prover-ready/verifier-ready split from af state, keeping the
M3-repair-wave validity semantics untouched (applies remain role === "verifier"
exactly, per-node, byte-re-bound). Then re-run the M3.5 baseline unchanged.

The alternative (re-scope M3.5 to a verify-only baseline over pre-seeded AISM proof
content) was considered and set aside: it would change the SC4 denominator's
meaning ("cost to re-prove" → "cost to re-verify") and contradict M3.5's committed
text. TJO can override; surfaced per the stop-condition rule, nothing picked
silently.

## Consequences

- M3.6's acceptance bar ("re-proves one AISM lemma end-to-end") is not yet met by
  the landed driver either — same missing wiring, same fix.
- The M3.9 SC4 comparison stays blocked behind the re-run baseline.
- No spend consumed from the TJO authorization; it remains in force for the re-run.
