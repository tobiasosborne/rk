<!-- ROLE: index + provenance note for the design record. AUTHORED. UPDATE POLICY:
     edited when documents are added or governance of this directory changes.
     TRIGGER: read via the CLAUDE.md §0 read order. -->

# docs/design — the rk design record

This directory is the AUTHORITATIVE home of the rk design record. It was moved
here whole from the sibling directory `../research-workflows/` on 2026-08-12
(rk-he3r: that directory was not a git repository, so a fresh clone of rk could
not follow its own read-order gate). Dated references to
`../research-workflows/` in memos, reviews, and bd issues written before
2026-08-12 refer to these files.

Contents, in CLAUDE.md §0 read order:

- `PRD.md` — WHAT is being built; decision record D1–D9 (settled) and
  Amendment A1 (autonomy north star, 2026-08-08).
- `IMPLEMENTATION_PLAN.md` — sequencing, work packages, acceptance bars,
  cross-repo items (V-series: vibefeld; F-series: knowledge-frontier), and the
  N-series addendum.
- `DISTILLATION.md` — the seven-repo synthesis that seeded the PRD.
- `NOTES-*.md` — dated working notes (append-only history; superseded content
  is superseded by later notes or by the PRD/plan, never edited in place).

Governance unchanged by the move: D1–D8 are settled (CLAUDE.md rule 1); the
PRD and plan are amended by dated appends with TJO sign-off, not rewritten.
