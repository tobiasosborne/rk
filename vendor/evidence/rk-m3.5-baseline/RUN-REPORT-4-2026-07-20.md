# M3.5 SC4-baseline — RUN-REPORT attempt 4 (raw data) — 2026-07-20

Companion to `STOP-REPORT-4-2026-07-20.md` (analysis + root cause). This file is the raw per-run
ledger. Outcome: **§7 stop — unseeded-workspace bootstrap deadlock; no SC4 denominator produced.**

- dist/rk: master tip `634dd31`. bun test 1840 pass/1 skip/0 fail; selftest OK 123/123 (green
  BEFORE build). Preflight (config 3/3 both dirs, af FU5 features, dry-run, in-band model
  resolution both directions) all passed. Cap: `--max-campaign-tokens 500000` on every live call.

## Per-lemma / per-run

| lemma | run | prover→verifier (resolved in-band) | status | applied nodes | prover turns | verifier turns | attributed tokens (in/out/cache_read/cache_creation) | cache frac | parity | wall-clock |
|---|---|---|---|---|---|---|---|---|---|---|
| lem-weighted-min | A | claude `claude-opus-4-8` → codex `gpt-5.6-sol` | aborted stuck-no-progress (3 rounds) | 0 | 0 | 3 | 69765 (39045/768/29952/0) | 0.4341 | N/A (no re-proof) | ~20-30s |
| lem-weighted-min | B | codex `gpt-5.6-sol` → claude `claude-opus-4-8` | aborted stuck-no-progress (3 rounds) | 0 | 0 | 3 | 69501 (6/1007/65839/2649) | 0.9612 | N/A (no re-proof) | ~20-30s |
| lem-mass-split | A/B | (run A cfg staged) | NOT DISPATCHED — identical unseeded shape → predicted identical failure; not run (spend discipline) | — | — | — | — | — | — | — |
| lem-starvation-completion-obstruction | A/B | (run A cfg staged) | NOT DISPATCHED — same | — | — | — | — | — | — | — |

Every verifier turn returned `accept` on the bare unseeded node → `cross-vendor-rejected`
(`identity-unparseable`, prover=unknown) → `node-skipped` → no progress → stuck. Same in both
directions. The prover half never fired (af marks the bare node verifier-ready, not prover-ready).

- **Baseline-campaign tokens total: 139266** (run A 69765 + run B 69501). Validated nodes: **0**.
  Applied verdicts: **0**. Neither run approached its 500k cap.
- **Baseline memos: NONE written** — 0 validated nodes ⇒ zero truthful memo entries (memo keys on
  validated af node ids; §8/§10(C)). A fabricated-row memo would misrepresent a run that produced
  no validated node.
- **Verdict-parity (§6): N/A for every lemma** — parity compares a completed fresh re-proof against
  the AISM original; no fresh re-proof completed (bootstrap deadlock), so there is nothing to
  compare. AISM was not read this session.

## rk-mq2 / rk-6nv

- **rk-mq2** (af accepts `batch_id:""` single-item applies?): unresolved — no `af apply` executed
  (all verdicts rejected pre-apply). Leave open.
- **rk-6nv** (prove→verify terminates on real lemma; challenge→prover-flip; refine-author
  recording): **negative** — prove→verify does not bootstrap on an unseeded workspace; verifier
  ACCEPTED (never challenged) both directions, so no prover-flip, no refine-author path reached.
  Leave open (with live evidence).

## Artifacts banked (all under ../rk-m3.5-baseline/)

- `_logs/lem-weighted-min.run-A.console.log`, `.run-A.driver-log.jsonl`, `.run-A.report.txt`
- `_logs/lem-weighted-min.run-B.console.log`, `.run-B.driver-log.jsonl`, `.run-B.report.txt`
- `_logs/lem-weighted-min.STOP3-stale.driver-log.jsonl` (archived pre-session)
- `_pristine/<lemma>/` — byte-clean workspace snapshots (used to reset between run A and run B)
- All three `proofs/<id>/` restored to pristine (1 pending node); run-A config active in all three.
</content>
