{
  "gate": "refs",
  "verdict": "fail",
  "findings": [
    {
      "severity": "ERROR",
      "path_pattern": "refs/records/spectral/L1-1.json",
      "message_pattern": "[stale-extraction]"
    }
  ],
  "exit_code": 1,
  "coverage": { "checked": 0, "total": 0, "unit_patterns": ["checked records: 1 L1 records / 1 reviewed-VALID / 2 anchors verified"] },
  "aism_behavior": "n/a",
  "notes": "rk-nsex, STALE EXTRACTION — the PDF never moved, its TEXT did. `sources/spectral.pdf` (refs-17's payload, reused unchanged) satisfies its adopted pin, and the lock's extraction chain is intact: `extraction.payload_sha256` equals the payload's current digest and `extraction.sha256` equals the sidecar's. The record, however, names a DIFFERENT `extraction_sha256` — the digest of the first extraction, made by an older pdftotext. Every line number in the record indexes that older sidecar. This is the failure the memo calls 'stale extraction' and the reason the record carries the extraction digest at all: re-extraction silently renumbers a document, so a record and a sidecar can be individually valid and jointly meaningless. Note again that the anchors still verify against today's sidecar (`2 anchors verified`) — luck, not evidence, and precisely why the check compares digests rather than re-reading the text. Because Gate 3's own resolver (Check 10) is satisfied here, refs-18/refs-19/refs-20 cannot detect this: they test the LOCK's chain, and this is the RECORD's chain to that lock. `aism_behavior: n/a`. Mutation-proof: dropping the extraction comparison in checkStaleness returns this fixture to zero findings, exit 0; observed and reverted. Sibling: refs-26 (the payload layer)."
}
