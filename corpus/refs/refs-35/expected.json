{
  "gate": "refs",
  "verdict": "fail",
  "findings": [
    {
      "severity": "ERROR",
      "path_pattern": "refs/records/widget-2026/L1-1.json",
      "message_pattern": "[statement-range-truncated]"
    }
  ],
  "exit_code": 1,
  "coverage": { "checked": 0, "total": 0, "unit_patterns": ["checked records: 1 L1 records / 1 reviewed-VALID / 1 anchors verified"] },
  "aism_behavior": "n/a",
  "notes": "rk-nsex Tier A repair wave, LANDING-BLOCKER BL2, the CAPITALISED-continuation half ('Capitalized or EOF-ending omitted clauses also pass'). The statement continues on the next line as `Consequently the widget graph must be d-regular.` — capitalised, so the pre-repair lowercase test did not see it, and NOT one of the continuation words either, so the word list would not have caught it under any spelling. What catches it is structural rather than lexical: the line is not separated from the range by a blank line and it begins no new labelled block, so it cannot be a new statement — the only three accepted terminators are a new labelled block, a blank line followed by a capitalised non-continuation sentence, and a declared EOF. The word `Consequently` is deliberately chosen to be OUTSIDE the continuation list: a fixture whose defect the word list happens to catch would prove the list, not the rule. `aism_behavior: n/a`. Mutation-proof: accepting any capitalised following line (dropping the blank-separation requirement) returns this tree to zero findings, exit 0; observed and reverted. Siblings: `refs-23` (lowercase continuation — the case the original heuristic did catch), `refs-34` (START), `refs-36` (EOF)."
}
