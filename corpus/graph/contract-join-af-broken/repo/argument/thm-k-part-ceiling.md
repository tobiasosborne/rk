---
id: thm-k-part-ceiling
kind: theorem
contract: The verified  sensitivity ceiling holds.
status: proved-mod-audit
af: seeded
workspace: proofs/thm-k-part-ceiling
---

Reviewer-B R4 reproduction: the configured af command answers with non-JSON, so the existing
workspace ledger cannot be used as the absent-binary fallback. The graph contract join is
therefore unresolved and must fail structurally on the obligatory `rk check` surface.
