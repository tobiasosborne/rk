---
id: thm-k-part-ceiling
kind: theorem
contract: The verified  sensitivity ceiling holds.
status: proved-mod-audit
af: seeded
workspace: proofs/thm-k-part-ceiling
---

Campaign-shaped contract-join escape: a banked `proved-mod-audit` result has a seeded af
workspace, and the graph's byte-exact registry-to-root join reports `contractMatch=false`.
Gate 2's older whitespace-normalized comparison treats the two statements as equal, so this
fixture was green under `rk check` before rk-45dj wired the graph conflict pass into that command.
