{
  "gate": "config",
  "verdict": "fail",
  "findings": [
    {
      "severity": "ERROR",
      "path_pattern": ".rk/config.json",
      "message_pattern": "phase: invalid value \"typo\""
    }
  ],
  "exit_code": 1,
  "coverage": { "checked": 0, "total": 1 },
  "aism_behavior": "n/a -- .rk/config.json and its runtime validation are an rk-only concept, no AISM script reads or validates it.",
  "notes": "rk-xbm (M1 review B1, docs/reviews/2026-07-18-m1-milestone-review-codex.md L1): reproduces the exact repro named in the finding -- a typo'd phase value in .rk/config.json. Before this bead, config-load.ts:39's unvalidated `as Partial<GateConfig>` cast let \"typo\" through unchecked, and src/gates/phase.ts:40's `if (phase === \"consolidation\")` treated ANY other value (including this typo) as exploration, silently demoting every non-structural ERROR across all six gates to WARN -- a silent severity-policy change (CLAUDE.md L6). `validateConfigOverrides` (src/gates/config.ts) now rejects this at the .rk/config.json-loading edge: `phase` falls back to the strict default \"consolidation\" (never silently exploration) and one loud, structural, non-demotable ERROR is produced. NOT wired into the generic corpus runner (src/corpus/run.ts / src/corpus/discovery.ts's `GATE_DIRS` is a fixed 6-entry tuple that does not include \"config\", and EXPECTED_FIXTURE_COUNT is out of this bead's file scope) -- proven instead by test/store/config-load.test.ts's `loadGateConfig -- rk-xbm` describe block and test/gates/config.test.ts, which load this exact fixture's repo/.rk/config.json shape directly. See src/corpus/discovery.ts: adding \"config\" to GATE_DIRS + bumping EXPECTED_FIXTURE_COUNT by 2 (this fixture + config-02) is the wiring a follow-up WP needs to make `bun run selftest` / `rk check --selftest` discover and run this directory automatically."
}
