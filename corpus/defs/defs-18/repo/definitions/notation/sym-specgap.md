---
id: sym-specgap
term: spectral gap
shard_type: notation
symbol: \specgap
class: spectral
expansion: \ensuremath{\Delta}
kind: consensus
consensus: campaign convention, qpcp.v1
status: locked
---

**The campaign's spectral gap**, lambda_1(H) - lambda_0(H): a property of ONE operator, never
of a promise problem. The translation row below names a source, a source symbol, and a locus —
and offers no bytes for any of it. `\Delta` is exactly the token that means a promise gap in
one paper and a spectral gap in another, so an unanchored claim here is the whole failure mode.

- aav-1309.7495: \Delta @ refs/aav-1309.7495/paper.tex:12

The line above is followed by prose, not by a byte-verbatim quote anchor.
