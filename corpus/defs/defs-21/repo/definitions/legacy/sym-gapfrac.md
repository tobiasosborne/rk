---
id: sym-gapfrac
term: absolute promise gap
kind: original
status: locked
consensus: superseded draft, never deleted
---

**A superseded entry left behind under a different directory**, defining the gap the OTHER
way round (absolute, not relative). Its `term` differs, so Gate 1's term/alias DRIFT check
sees no collision; only the flat `id` collides. Every consumer — Gate 2's `defs:`, the live
verifier's definition context, the rendered definitions view — resolves a definition by id
ALONE, so which of these two a reader gets is decided by scan order.
