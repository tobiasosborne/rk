{
  "gate": "freshness",
  "verdict": "fail",
  "findings": [
    { "severity": "ERROR", "path_pattern": ".rk/generated.json", "message_pattern": "unrecognized top-level property" },
    { "severity": "ERROR", "path_pattern": ".rk/generated.json", "message_pattern": "\"extra_top\"" }
  ],
  "exit_code": 1,
  "coverage": { "checked": 0, "total": 0, "unit_patterns": ["generated artifacts"] },
  "aism_behavior": "n/a — schemas/generated.v1.json is rk-only (M2.6); no AISM equivalent.",
  "notes": "M2 boundary review blocker #4: schemas/generated.v1.json declares additionalProperties:false at the top level — the pre-repair runtime parser silently accepted any extra top-level key. Proves this is now a loud, counted ERROR. Mutation-proven red-first (temporarily removing the extra-top-level-key check turns this fixture green when it must be red; reverted after confirming)."
}
