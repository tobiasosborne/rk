{
  "gate": "linker",
  "verdict": "fail",
  "findings": [
    {
      "severity": "ERROR",
      "path_pattern": "argument/thm-qpcp.md",
      "message_pattern": "[regime-unentailed]"
    },
    {
      "severity": "ERROR",
      "path_pattern": "argument/thm-qpcp.md",
      "message_pattern": "dependency 'lem-amp' requires the ambient regime 'qdim' = 'poly'"
    }
  ],
  "exit_code": 1,
  "config_override": {
    "signatures": "required",
    "conventionProfile": "rk-corpus.v1"
  },
  "aism_behavior": "class-driven (no AISM counterpart -- AISM's argument.py has no signature vocabulary, no convention profile, and no regime reasoning of any kind).",
  "notes": "THE red fixture of rk-8805, and the exact pair the 2026-08-20 Tier A review (LB2) broke v1's signature check with: `lem-amp` holds at qdim=poly and produces gap=const; `thm-qpcp` runs at qdim=const and consumes gap=const AND qdim=const. Atom-wise matching PASSED it -- the gap atom came from the dependency, the dimension atom from the parent's OWN regime -- so a result proved only for polynomial-dimensional qudits silently backed a constant-dimension theorem. Under interval entailment the context's qdim interval ['const','const'] is not CONTAINED in the required ['poly','poly'], so lem-amp is never made available and its post never joins the context. Structural: admission is phase-independent (memo section 2a / review LB4).",
  "coverage": {
    "checked": 2,
    "total": 2,
    "unit_patterns": [
      "signatures: 2 shards / 2 routes / 1 entailment"
    ]
  }
}
