{
  "gate": "linker",
  "verdict": "fail",
  "findings": [
    {
      "severity": "ERROR",
      "path_pattern": "argument/lemmas/lem-no-kind.md",
      "message_pattern": "missing required field 'kind'"
    }
  ],
  "exit_code": 1,
  "aism_behavior": "differs: argument.py's parse_registry validates `kind` only when `fm.get(\"kind\")` is truthy (`if fm.get(\"kind\") and fm[\"kind\"] not in KINDS:`, argument.py:141, current checkout), so a shard with no `kind:` line at all registers with zero findings. Script-verified (rk-4uw, 2026-07-18) via the module-import harness described in corpus/README.md's Validation methodology: `argument.exec_module` imported read-only, ARG_DIR/DEFS_DIR/PROOFS_DIR patched to this fixture's repo/, `parse_registry(ARG_DIR)` called directly — returns `lem-no-kind` with `errors == []`; the full non-generate composition (argument.py:679-702, check_acyclic/check_imports/check_status/check_contracts/check_orphans/check_generated/check_brittleness) also produces zero errors and zero warnings, exit 0. Confirms ruling (d) empirically (2026-07-18 re-review ratification cited `argument.py:139`; the exact line is 141 in this checkout — a two-line drift, same statement).",
  "notes": "[rk-aft, 2026-07-18 M0.3 milestone review finding 3] gate-contracts.md:303 marks `kind` required (`yes`), matching `id`'s required-field row. Pre-fix, linker-parse.ts:125-126 gated the enum check on `if (kind && !KINDS.has(kind))`, so an ABSENT (or empty-string) `kind` short-circuited the condition and produced zero findings — the lemma still registered (1/1 coverage) with a fully consistent generated INDEX/DAG (kind renders as `?` via linker-render.ts:57, same as any other missing-optional-field cell). Fix mirrors the 'id' required-field check's message convention (missing required field 'X') while keeping the existing enum-check's allowed-list phrasing for a present-but-invalid value; unlike missing `id` (which excludes the shard from `lemmas` entirely, per the file header's [F12] divergence), a missing `kind` does NOT exclude the shard — `kind` is optional on the `Lemma` type and every other check must still see this shard, matching how invalid-but-present `status`/`af` do not exclude either. Sanity-checked against AISM (`../almost-idempotent-stochastic-maps`): no live shard lacks `kind:`, so this is a real gap class, not an observed production divergence. Triage: rk-stricter-intended (confirms ratified ruling (d), 2026-07-18 re-review: AISM checks the enum only when `kind` is truthy)."
}
