{
  "gate": "linker",
  "verdict": "fail",
  "findings": [
    {
      "severity": "ERROR",
      "path_pattern": "argument/thm-loose.md",
      "message_pattern": "[regime-unentailed]"
    },
    {
      "severity": "ERROR",
      "path_pattern": "argument/thm-loose.md",
      "message_pattern": "'qdim' = [*, const]"
    }
  ],
  "exit_code": 1,
  "config_override": {
    "signatures": "required",
    "conventionProfile": "rk-corpus.v1"
  },
  "aism_behavior": "class-driven (no AISM counterpart -- AISM's argument.py has no signature vocabulary, no convention profile, and no regime reasoning of any kind).",
  "notes": "The CAPPED reading of a parameter, and the reason predicate values are INTERVALS rather than points with a per-key polarity flag (codex Tier A review of the convention-profile draft, findings 10-11). `lem-const-dim` needs the dimension capped at a constant: its requirement is the interval [*, const]. `thm-loose` runs at qdim=poly, whose point interval is NOT contained in it, so the lemma is unavailable. The SAME containment rule produces linker-47's afforded-style rejection -- one rule, both readings, and no key needs a direction flag that could be set wrongly."
}
