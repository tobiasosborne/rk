# argument/lemmas (empty, legacy-shape placeholder)

Prose documentation, not a shard. Excluded from the recursive shard scan by name
(rk-9pk), same as the sibling `argument/README.md`, at a different depth.
