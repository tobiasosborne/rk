---
id: lem-stage1-smooth-unitary-operations
kind: lemma
status: proved-mod-audit
af: none
contract: Stage-1 smooth unitary operations compose to within the stated error budget.
---

The incident's SECOND retracted proof, carried on the L5 soft tier instead of af. Its
`.rk/l5-verdicts.jsonl` record is a FRESH `VALID` — bound to this file's current raw bytes, so
nothing about edit-triggered staleness fires — and the shard is labeled `proved-mod-audit` on
the strength of it. The retraction record (`l5-shard-bytes` domain) is pinned to those same
current bytes, so it is LIVE and overrides the verdict: Gate 2 Check 14 ERRORs and calls for a
demote or a re-verification. This is the half of the incident that no hash comparison could ever
have caught, because the bytes never changed.
