{
  "northStarId": "star",
  "workers": {
    "assignments": {
      "prover": { "hard": { "backend": "corpus-fake", "fallbacks": [] } },
      "verifier": { "hard": { "backend": "corpus-fake", "fallbacks": [] } }
    }
  }
}
