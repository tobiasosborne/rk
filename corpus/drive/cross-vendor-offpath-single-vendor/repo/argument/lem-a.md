---
id: lem-a
kind: lemma
contract: lem-a holds.
af: seeded
workspace: proofs/lem-a
---

A claim with a real af workspace that no dep/route path from `star` reaches. PRD C9's
"Non-critical-path: same-family allowed, recorded" branch applies here — and was unreachable while
`src/cli/verify-live.ts` hard-coded `isLoadBearing: () => true` (bead rk-bun).
