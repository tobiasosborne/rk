<!-- ROLE: current state. UPDATE POLICY: rewritten whole at every session close (≤ 80 lines);
     never appended. TRIGGER: read first at session start; re-read after any compaction —
     the repo is the source of truth, the conversation is not. -->

# STATE — as of YYYY-MM-DD (wave N)

## Headline
One paragraph: where thm-main stands (effective status, conditional on which assumptions),
in the exact words the banner prints. Nothing stronger.

## What moved this session
- claim id: old status -> new status (why; note path)

## Next steps (ordered)
1.

## Decisions needed from the user
-

## Gate state
`make check`: PASS/FAIL (N ERROR, M WARN). `make build`: PASS/FAIL, P pages.
