<!-- ROLE: notation and conventions, fixed at FIRST USE. UPDATE POLICY: append-only entries;
     changing a fixed convention is a dated entry plus a sweep of every file that used the old
     one. "All scientific bugs are convention bugs until proved otherwise." -->

# CONVENTIONS

## Fixed (entry = symbol/choice, value, where first used, date)
- (example) Hilbert-space inner product: linear in the SECOND argument. First used: def-state. 2026-01-01.

## Unfixed — do not silently choose any of these
List the choices that have not been made yet (sign of a Hamiltonian, log base, normalisation
of a norm, index ranges). A worker that needs one of these stops and reports instead of picking.
