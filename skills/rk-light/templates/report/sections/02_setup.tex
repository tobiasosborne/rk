% ROLE: setup. Conventions (mirrors CONVENTIONS.md), definitions (one \label{def:...} each, a
% `def` row each, `quoted` ones backed by a PROVENANCE row), assumptions (asm: labels), and the
% cited facts (cit: labels, every one backed by a PROVENANCE row).
\section{Setup and conventions}\label{sec:setup}

\subsection{Conventions}
State sign, normalisation, and naming conventions here exactly as fixed in CONVENTIONS.md.

\subsection{Definitions}
% \begin{definition}\label{def:example}
% One definition per environment; never restate it elsewhere.
% \end{definition}

\subsection{Assumptions}
% \begin{assumption}\label{asm:example}\claimstatus{asm:example}{assumed}
% An assumption the proofs rely on that is NOT established here or in a source. Every
% conditional headline names these by id.
% \end{assumption}

\subsection{Cited facts}
% \begin{theorem}[Source, Thm 1]\label{cit:example}\claimstatus{cit:example}{cited}
% Reproduce the statement; the PROVENANCE row pins the verbatim quote.
% \end{theorem}

\section{Dead routes}\label{sec:dead-routes}
Mirrors DEAD-ROUTES.md: route, the wall it hit, the evidence. A dead route is a result.

\section{Open problems}\label{sec:open}
Everything \textsf{open}, \textsf{conjectured} or \textsf{unsupported}, plus audit follow-ups.
